import subprocess

# Installez moviepy
subprocess.run(['pip3', 'install', 'moviepy'])

# Installez opencv-python
subprocess.run(['pip3', 'install', 'opencv-python'])

# Installez speech_recognition
subprocess.run(['pip3', 'install', 'SpeechRecognition'])

# Installez nltk
subprocess.run(['pip3', 'install', 'nltk'])

# Installez transformers (pytorch-transformers)
subprocess.run(['pip3', 'install', 'transformers'])

# Installez reportlab
subprocess.run(['pip3', 'install', 'reportlab'])
